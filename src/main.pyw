from gui.window import SecurityAppGUI

def main():
    # Создаем объект нашего графического интерфейса
    app = SecurityAppGUI()
    # Запускаем окно на экран
    app.run()

if __name__ == "__main__":
    main()
